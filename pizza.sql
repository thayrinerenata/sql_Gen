create database db_pizzaria_legal;
use db_pizzaria_legal;
create table tb_pizza(
	id bigint (5) auto_increment,
    sabor varchar (30),
    borda varchar (30),
    adicional varchar (30),
    primary key (id)
);
create table tb_categoria(
	id bigint (5) auto_increment,
    preco float (10),
    nome varchar (30)
    
);   


insert into tb_() values ();

select * from tb_ where  > 2000;
select * from tb_ where  < 2000;
 